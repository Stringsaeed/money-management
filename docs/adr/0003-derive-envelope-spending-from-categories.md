# Derive Envelope spending from Categories

Posted expense Transactions spend from the Envelope associated with their Category rather than storing or selecting an Envelope on every Transaction. Each Category therefore belongs to at most one Envelope, which preserves the existing Transaction-entry flow and prevents one expense from being counted more than once.
