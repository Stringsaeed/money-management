# Version Category mappings by Budget Period

A Category Mapping changed during a Budget Period applies to that entire period and future periods, while earlier periods keep the mapping they used unless the user explicitly requests a backfill. Applying only the latest mapping to every Transaction would silently rewrite reviewed budget history, while waiting until the next period would make current-month corrections needlessly difficult.
