import { styled } from "nativewind";
import { NumberFlow as NumberFlowPrimitive } from "number-flow-react-native";

const NumberFlow = styled(NumberFlowPrimitive, {
  className: "style",
  containerClassName: "containerStyle",
});

export { NumberFlow };
