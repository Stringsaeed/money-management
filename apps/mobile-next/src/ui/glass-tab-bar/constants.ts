export const PILL_PADDING = 6;
export const TAB_WIDTH = 50;
export const TAB_HEIGHT = 44;
export const CREATE_SIZE = 58;

/** Extra fade height above the tab bar that hints at scrollable content beneath it. */
export const SCROLL_FADE_OVERSCAN = 48;

/** Theme background colors the scroll fade blends into (mirrors --color-background in global.css). */
export const BACKGROUND_COLORS = {
  light: "#f5f5f0",
  dark: "#0f1a14",
} as const;
