{
  "name": "money-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run build && pnpm -C apps/mobile start",
    "reset-project": "pnpm -C apps/mobile reset-project",
    "android": "turbo run build && pnpm -C apps/mobile android",
    "ios": "turbo run build && pnpm -C apps/mobile ios",
    "web": "turbo run build && pnpm -C apps/mobile web",
    "prebuild": "turbo run build && pnpm -C apps/mobile prebuild",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check-loose-end": "turbo run build && pnpm -C apps/mobile check-loose-end",
    "test": "turbo run build && pnpm -C apps/mobile test",
    "test:watch": "turbo run build && pnpm -C apps/mobile test:watch",
    "test:coverage": "turbo run build && pnpm -C apps/mobile test:coverage",
    "test:ci": "turbo run build && pnpm -C apps/mobile test:ci",
    "ts:check": "turbo run build && pnpm -C apps/mobile ts:check",
    "push:storage": "node apps/mobile/scripts/push-code-storage.mjs",
    "dev:server": "pnpm --filter @trove/infra dev",
    "db:generate": "turbo run db:generate -F @trove/db --",
    "deploy": "turbo run deploy -F @trove/infra --",
    "destroy": "turbo run destroy -F @trove/infra --"
  },
  "devDependencies": {
    "@oxlint/plugins": "1.80.0",
    "@rnx-kit/oxlint-config": "^1.0.3",
    "@swmansion/argent": "^0.20.0",
    "eslint": "^10.8.1",
    "eslint-plugin-expo": "^1.1.0",
    "oxfmt": "^0.63.0",
    "oxlint": "1.80.0",
    "turbo": "^2.10.11"
  },
  "packageManager": "pnpm@11.24.0"
}
