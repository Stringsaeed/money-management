config:
  edition: 3

streams:
  memberships:
    auto_subscribe: true
    priority: 1
    query: |
      SELECT * FROM membership
      WHERE user_id = auth.user_id()

  # A signed-in User always has a Personal Ledger, so this streams eagerly and
  # needs no subscription parameter: the ledger row itself names its owner.
  personal_ledger:
    auto_subscribe: true
    priority: 2
    accept_potentially_dangerous_queries: true
    queries:
      - |
        SELECT * FROM accounts
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM categories
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM transactions
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM budget_workspaces
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM envelopes
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM category_mappings
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM funding_memberships
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM rollover_settings
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM assignments
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM refund_links
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
      - |
        SELECT * FROM recurring_rules
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
          AND (
            account_id IS NULL OR account_id IN (
              SELECT id FROM accounts
              WHERE ledger_id IN (
                SELECT id FROM ledger
                WHERE personal_user_id = auth.user_id()
              )
                AND (visibility = 'public' OR owner_user_id = auth.user_id())
            )
          )
          AND (
            to_account_id IS NULL OR to_account_id IN (
              SELECT id FROM accounts
              WHERE ledger_id IN (
                SELECT id FROM ledger
                WHERE personal_user_id = auth.user_id()
              )
                AND (visibility = 'public' OR owner_user_id = auth.user_id())
            )
          )
      - |
        SELECT * FROM recurring_occurrences
        WHERE ledger_id IN (
          SELECT id FROM ledger
          WHERE personal_user_id = auth.user_id()
        )
          AND rule_id IN (
            SELECT id FROM recurring_rules
            WHERE ledger_id IN (
              SELECT id FROM ledger
              WHERE personal_user_id = auth.user_id()
            )
              AND (
                account_id IS NULL OR account_id IN (
                  SELECT id FROM accounts
                  WHERE ledger_id IN (
                    SELECT id FROM ledger
                    WHERE personal_user_id = auth.user_id()
                  )
                    AND (visibility = 'public' OR owner_user_id = auth.user_id())
                )
              )
              AND (
                to_account_id IS NULL OR to_account_id IN (
                  SELECT id FROM accounts
                  WHERE ledger_id IN (
                    SELECT id FROM ledger
                    WHERE personal_user_id = auth.user_id()
                  )
                    AND (visibility = 'public' OR owner_user_id = auth.user_id())
                )
              )
          )

  household_ledger:
    accept_potentially_dangerous_queries: true
    queries:
      - |
        SELECT * FROM accounts
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
          AND (visibility = 'public' OR owner_user_id = auth.user_id())
      - |
        SELECT * FROM categories
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM transactions
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
          AND account_id IN (
            SELECT id FROM accounts
            WHERE household_id = subscription.parameter('household_id')
              AND (visibility = 'public' OR owner_user_id = auth.user_id())
          )
          AND (
            to_account_id IS NULL OR to_account_id IN (
              SELECT id FROM accounts
              WHERE household_id = subscription.parameter('household_id')
                AND (visibility = 'public' OR owner_user_id = auth.user_id())
            )
          )

  household_budget:
    accept_potentially_dangerous_queries: true
    queries:
      - |
        SELECT * FROM budget_workspaces
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM envelopes
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM category_mappings
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM funding_memberships
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM rollover_settings
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM assignments
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
      - |
        SELECT * FROM refund_links
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )

  household_recurring:
    accept_potentially_dangerous_queries: true
    queries:
      - |
        SELECT * FROM recurring_rules
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
          AND (
            account_id IS NULL OR account_id IN (
              SELECT id FROM accounts
              WHERE household_id = subscription.parameter('household_id')
                AND (visibility = 'public' OR owner_user_id = auth.user_id())
            )
          )
          AND (
            to_account_id IS NULL OR to_account_id IN (
              SELECT id FROM accounts
              WHERE household_id = subscription.parameter('household_id')
                AND (visibility = 'public' OR owner_user_id = auth.user_id())
            )
          )
      - |
        SELECT * FROM recurring_occurrences
        WHERE household_id = subscription.parameter('household_id')
          AND household_id IN (
            SELECT household_id FROM membership
            WHERE user_id = auth.user_id()
          )
          AND rule_id IN (
            SELECT id FROM recurring_rules
            WHERE household_id = subscription.parameter('household_id')
              AND (
                account_id IS NULL OR account_id IN (
                  SELECT id FROM accounts
                  WHERE household_id = subscription.parameter('household_id')
                    AND (visibility = 'public' OR owner_user_id = auth.user_id())
                )
              )
              AND (
                to_account_id IS NULL OR to_account_id IN (
                  SELECT id FROM accounts
                  WHERE household_id = subscription.parameter('household_id')
                    AND (visibility = 'public' OR owner_user_id = auth.user_id())
                )
              )
          )
