# React Native / Expo require a flat node_modules layout for Metro,
# CocoaPods, and Gradle to resolve packages correctly.
node-linker=hoisted
shamefully-hoist=true

# Honor the engines.node field in package.json
engine-strict=true

# Match yarn's behavior: do not auto-install peer deps silently
auto-install-peers=true
